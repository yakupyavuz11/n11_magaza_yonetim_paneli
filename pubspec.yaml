name: n11_magaza_yonetim_paneli
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:

  charts_flutter: ^0.12.0
  cupertino_icons: ^1.0.6
  fl_chart: ^0.69.0
  flutter:
    sdk: flutter

dev_dependencies:
  echart_flutter: ^0.1.0+2
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/ # Images için doğru klasör yolu
  # Eğer özel fontlar kullanıyorsan, buraya ekleyebilirsin.
  # Örnek font ekleme:
  # fonts:
  #   - family: MyFont
  #     fonts:
  #       - asset: assets/fonts/MyFont-Regular.ttf
